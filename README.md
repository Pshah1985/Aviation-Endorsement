# Aviation Endorsement UI

Interactive Endorsement screen implementation with cascading filters, add-to-grid behavior, detail modal, delete confirmation, and pagination.

## Run locally

```bash
npm install
npm start
```

App runs on Vite (aliased to `npm start`) and serves the Endorsement page at `http://localhost:5173` by default.
